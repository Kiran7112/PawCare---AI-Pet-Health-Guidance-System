# Base URL for the PawCare+ API.
# Leave empty to use same-origin "/api" (the Vite dev proxy forwards to :8000).
VITE_API_BASE_URL=

# Dev-only: where the Vite proxy forwards /api requests.
VITE_API_PROXY_TARGET=http://localhost:8000
